
<%- include("partials/header") -%> 

<h1>Home</h1>
<p><%= startingContent %> </p>

<!-- for(let i = 0 ; i<posts.length ; i++ ){ 
console.log(posts[i].title);
 } -->

 <!-- OR -->
 <!-- In new version of javascript forEach is used which just like loop but much simple -->
 <% posts.forEach(function(post){ %> 
    <h1><%= post.title %></h1>
    <p><%= post.body.substring(0,100) +"..."%>
    <a href="/posts/<%= post.title %> " class="readMore">Read More</a></p>
<% }); %> 




<%- include("partials/footer") -%> 